export interface AgGridStateControlsProps {
	onReset: () => void;
	resetTitle?: string;
}

export const AgGridStateControls = ({
	onReset,
	resetTitle = "Сбросить настройки таблицы",
}: AgGridStateControlsProps) => {
	return null;
};
