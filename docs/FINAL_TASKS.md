**1 Итерация - Пользовательский интерфейс для просмотра Линейджа**

| **План** | **Статус** | **Срок** | **Описание, кейс, ожидаемый результат** | **ТЗ** | **Ссылка dev** | **Комментарий** |
| --- | --- | --- | --- | --- | --- | --- |
| **1\. Запрос и формирование JSON текущей версии модели данных из РБД DL** |     |     | **При входе в приложении происходит запрос в РБД DL для инициализации данных и сборки итогового json для отправки на фронт.**<br><br>**(НЕ для 1 итерации- для последующего сохранения/обновления этого json'a).** |     |     |     |
| **2\. Главный (начальный) экран, Поиск по объектам модели (модели, витрины)** |     |     | **На главном экране строка поиска и дашборд (может в виде таблицы) со списком объектов (источники, витрины, json и тд), открываем карточку объекта (п.3).** | [Начальный экран, Поиск по объектам модели (модели, витрины)https://sfera.inno.local/knowledge/pages?id=1751565](https://sfera.inno.local/knowledge/pages?id=1751565) |     | <br><br>**Что такое сущности и объекты?**<br><br>**Что за граф открывается сразу?**<br><br>**Как лучше объединить связанные маппинги и детали? Чем отличаются?**<br><br>**Атрибуты дублируются и н карточке и в табличном представлении, зачем?** |
| **3\. Карточка объекта (Виджет просмотра витрин)** |     |     | **В карточке объекта есть описание модели - тип, наименование, описание, пространство данных. и 2 вкладки:** |     |     | **Добавить описание объекта? Что есть сейчас и что должно быть? Югович** |
| **4\. Виджет просмотра атрибутов/связей объекта в виде таблицы** |     |     | **Первая - открывается сразу и показывает "атрибуты" модели с описанием с фильтром/чекрыжем для перехода на "связи" (или 2 вкладки?). В связах должна быть возможность открыть нужную связь для просмотра маппинга/функции (смотри пункт 4в)** |     |     |     |
| **4а. Граф, отображение всех объектов в линейдж модели** |     |     | **Вторая вкладка - отображение графа связей (краткая схема, только связи между объектами и небольшим кол-ом содержимого, 3-5 полей или только основные, в котором тоже можно посмотреть описание атрибутов/связей выбранного объекта на графе в отдельном всплывающем окне (примерно как сейчас).** |     |     | **Нужно ли скрывать/показывать все атрибуты? Югович**<br><br>**Витя - выяснить максимальное/среднее кол-во атрибутов**<br><br>**Можно открывать не по всем, а только по тем, где есть связи?**<br><br>**Целевой вариант, чтобы все связи были? Сделать 2 режима - связи с таблицами и все связи атрибутов.** |
| **4б. Возможность раскрыть детализацию (состав полей) по интересующему объекту в линейдже** |     |     | **На ней же есть возможность через чекрыж/кнопку перейти на подробный граф со связями всех атрибутов** |     |     | **Добавить возможность открытия связи выбранного атрибута для просмотра маппинга/функции.**<br><br>**При клике на линию связи (edge) появляется маппинг атрибутов - атрибут источника с атрибутом цели и описанием.** |
| **4в. Возможность просмотреть детализацию связи объектов линейджа (состав полей участвующих в выбранной линии трансформации) с возможность просмотра логики трансформации** |     |     | **При выборе нужной связи открывает отдельное окно в котором есть возможность посмотреть 2 типа данных (по чекрыжу):**<br><br>**1\. Маппинг атрибутов этой связи.**<br><br>**2\. Описании функции атрибутов источника.** |     |     |     |

**Риск -** сейчас работаем не с итоговым форматом файла, а синтетикой
