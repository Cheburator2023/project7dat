1. При скролле на последний элемент в списке наименования процесса этот элемент отображается на половину. Не столь критично, но можно поправить. (добавить в конец списка паддинг)

2. На ИФТ данные перестали прогружаться. Это при первом заходе на ui.
   Если переключиться допустим на сервис реестр коммитов и обратно, то сущности появятся. (Проверить почему первые запросы не идут, добавить в react query retries 5 при падающих запросах, добавить задержку в 5 сек чтобы не гонять запросы слишком быстро подряд)

3. Если в системе подгружен и сохранен хотя бы один несмерженный файл коммита, то новый загрузить нельзя. Можно только удалить его и загрузить по новой. (убрать подгрузку нового коммита в коммит уже в processing.)

4. Фронт системы выполняет предварительные проверки загружаемого файла на соответствие формату ( S2T) и на максимальный размер (< 1 Мб)

4.1. Если файл не проходит предварительную проверку на фронте пользователю отображается диалоговое окно с сообщением об ошибке ("Тип файла не является корректным, загрузите новый файл"/"Превышен максимальный размер файла, загрузите новый файл"). Файл не загружается. Сценарий завершается.4.2. Ели файл проходит предварительную проверку на фронте файл направляется на проверку в бэк-сервис

5. фича. перед мерджем создать модалку где будет показан весь дашборд графа с таблицей и графом сущностей (как на главной), где можно будет предварительно посмотреть как будут выглядеть сущности после мерджа. заменить кнопку межда в контектном меню на эту фичу и переместить мердж уже в модалку на кнопку подтверждения.
добавленные сущности в модалке (в панелях) должны быть отмечены как добавленные/измененные цветом. мб в таблице их можно закрепить, а остальные данные по сущности в графе подсветить.