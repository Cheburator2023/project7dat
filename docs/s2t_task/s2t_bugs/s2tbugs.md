
1. страница импорта s2t - Добавить возможность ввода текста вручную, чтоб не мешала всплывалка. переделать выбор процесса в селекте. переместить всплывающее окно селекта ниже инпута и сделать его блочны относительным позиционированием как часть потока блоков а не абсолютным окном поверх.

2. страница импорта s2t - в селекте Подтягивать описание процесса после его выбора (можно показывать на строчке перед выбором)

3. страница общая коммитов - Выделить явно статусы, done зеленый, failed красный, processing желтый - перевести на русский на ui, title на оригинальном
4. страница общая коммитов - Статус processing может быть только один, если есть хоть один коммит с таким статусом, то кнопку Импорт s2t дизейблить

5. страница общая коммитов - Кнопку "Импорт s2t" сделать более явной как кнопку (поменять вариант) 
6. страница общая коммитов - Убрать колонку change_id
7. страница общая коммитов - Попробовать загрузить все 3 вида s2t. возможно проебались когда писали код для импорта надо проверить и добавить новый код для 3-х типов импортируемых файлов (Соответствие полей JSON полям S2T):
    a.  Пример S2T витрины - файл в корне репы docs/s2t_task/s2t_files_examples_jsons/prod_dm_dadm_pvr.pd_mb_behaviour_online.json
    b.  Пример S2T JSON файла (с2т с json) - файл в корне репы docs/s2t_task/s2t_files_examples_jsons/json_S2T_пример JSON.json
    c.  Пример S2T модели - файл в корне репы docs/s2t_task/s2t_files_examples_jsons/model_S2T_пример модели.json

    
8. Новая доработка JSON (не баг) . Добавить пустые незаполняемые поля в схему JSON коммита чтобы соответствовал JSOB основы, это значительно облегчит мёрджинг. В спецификации пустые поля добавлены серым цветом, их заполнять из s2t не надо, но они должны присутствовать в коммите с пустыми значениями.

9. В реестре нужны кнопки(управляющие элементы/контекстное меню):
    a. Для вызова окна редактирования метаданных (название, описание коммита)
    b. Для вызова окна  редактирования  json коммита с диффом к оригиналу коммита.
    c. Для запуска  процедуры и окон мерджинга.


10. отчет json -  в корне репы docs/s2t_task/docx_md/4.2. Отчёт JSON-9.html
замечания:

1. при парсинге смёрдженного json на РБД надо знать какой тип коммита был смёржен,, это влияет на алгоритм.
Добавить в заголовок json поле с типом применённого коммита desc.commit_type. 

2. в сценариях коммитов ещё добавил что и для s2t модели model_* при загрузке надо запрашивать процесс и записывать в полученный посте конвертации json. Для s2t с  json файлом  json_* процесс по прежнему не требуется запрашивать, только имя и описание коммита.

